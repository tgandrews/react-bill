'use strict';

let React = require('react/addons');

// CSS
require('normalize.css');
require('../styles/main.css');

var SkyBillApp = React.createClass({
  render: function() {
    return (
      <h1>Hello world</h1>
    );
  }
});

module.exports = SkyBillApp;
